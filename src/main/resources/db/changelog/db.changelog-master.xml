<?xml version="1.0" encoding="utf-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

    <!--
    Changelog principal pour l'application Maritime SVS
    Dakar - Sénégal
    -->

    <!-- Configuration générale -->
    <property name="now" value="now()" dbms="h2"/>
    <property name="now" value="current_timestamp" dbms="postgresql"/>
    <property name="floatType" value="float4" dbms="postgresql, h2"/>
    <property name="floatType" value="float" dbms="mysql, oracle, mssql, mariadb"/>
    <property name="clobType" value="longvarchar" dbms="h2"/>
    <property name="clobType" value="clob" dbms="mysql, oracle, mssql, mariadb"/>
    <property name="clobType" value="text" dbms="postgresql"/>
    <property name="uuidType" value="uuid" dbms="postgresql"/>
    <property name="uuidType" value="uniqueidentifier" dbms="mssql"/>
    <property name="uuidType" value="RAW(16)" dbms="oracle"/>

    <!-- Changeset initial pour tester la configuration -->
    <changeSet id="000001" author="svs-maritime">
        <comment>Création table de test pour vérifier la configuration Liquibase</comment>
        <createTable tableName="liquibase_test">
            <column name="id" type="BIGINT" autoIncrement="true">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="test_name" type="VARCHAR(255)">
                <constraints nullable="false"/>
            </column>
            <column name="created_at" type="TIMESTAMP" defaultValueComputed="${now}">
                <constraints nullable="false"/>
            </column>
        </createTable>
    </changeSet>

    <!-- Suppression de la table de test -->
    <changeSet id="000002" author="svs-maritime">
        <comment>Suppression de la table de test</comment>
        <dropTable tableName="liquibase_test"/>
    </changeSet>

    <!-- Les futurs changelogs seront ajoutés ici -->

</databaseChangeLog>