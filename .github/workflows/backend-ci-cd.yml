name: CI/CD Backend Staging - SVS Maritime

on:
  push:
    branches: [ develop ]
  pull_request:  # Ajout pour tester les PR
    branches: [ develop ]
  workflow_dispatch:  # Permet de déclencher manuellement le workflow

env:
  JAVA_VERSION: '17'
  JAVA_DISTRIBUTION: 'temurin'
  MAVEN_OPTS: '-Xmx1024m'
  ARTIFACT_NAME: 'svs-spring-app'
  DEPLOY_PATH: '/home/deployer'

jobs:
  # Job de validation des prérequis
  validate:
    runs-on: ubuntu-latest
    outputs:
      should-deploy: ${{ github.ref == 'refs/heads/develop' && github.event_name != 'pull_request' }}
    steps:
      - name: Validation du contexte
        run: |
          echo "Branch: ${{ github.ref }}"
          echo "Event: ${{ github.event_name }}"
          echo "Should deploy: ${{ github.ref == 'refs/heads/develop' && github.event_name != 'pull_request' }}"

  # Job de build avec tests et sécurité
  build:
    runs-on: ubuntu-latest
    needs: validate
    steps:
      - name: Checkout du code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Pour SonarQube et analyse complète

      - name: Setup Java ${{ env.JAVA_VERSION }}
        uses: actions/setup-java@v4
        with:
          java-version: ${{ env.JAVA_VERSION }}
          distribution: ${{ env.JAVA_DISTRIBUTION }}
          cache: maven

      - name: Cache des dépendances Maven
        uses: actions/cache@v4
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
          restore-keys: |
            ${{ runner.os }}-maven-

      - name: Vérification de la configuration Maven
        run: |
          mvn --version
          mvn help:effective-settings
          mvn help:active-profiles

      - name: Exécution des tests unitaires
        run: mvn -B test -Pstaging
        env:
          MAVEN_OPTS: ${{ env.MAVEN_OPTS }}

      - name: Build de l'application
        run: |
          mvn -B clean package -Pstaging \
            -DskipTests=false \
            -Dmaven.test.failure.ignore=false
        env:
          MAVEN_OPTS: ${{ env.MAVEN_OPTS }}

      - name: Vérification de l'existence de l'artefact
        run: |
          if [ ! -f target/*.jar ]; then
            echo "❌ Aucun fichier JAR trouvé dans target/"
            ls -la target/
            exit 1
          fi
          echo "✅ Artefact JAR trouvé:"
          ls -la target/*.jar

      - name: Génération du rapport de build
        run: |
          echo "## 📊 Rapport de Build" > build-report.md
          echo "- **Date**: $(date)" >> build-report.md
          echo "- **Branch**: ${{ github.ref_name }}" >> build-report.md
          echo "- **Commit**: ${{ github.sha }}" >> build-report.md
          echo "- **Java Version**: ${{ env.JAVA_VERSION }}" >> build-report.md
          echo "### Artefacts générés:" >> build-report.md
          ls -la target/*.jar >> build-report.md

      - name: Upload de l'artefact JAR principal
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.ARTIFACT_NAME }}
          path: target/*.jar
          retention-days: 30
          compression-level: 6
          if-no-files-found: error

      - name: Upload des rapports de tests
        uses: actions/upload-artifact@v4
        with:
          name: test-reports
          path: |
            target/surefire-reports/
            target/site/
          retention-days: 7
          if-no-files-found: warn

      - name: Upload du rapport de build
        uses: actions/upload-artifact@v4
        with:
          name: build-report
          path: build-report.md
          retention-days: 7

  # Job de déploiement conditionnel
  deploy:
    runs-on: ubuntu-latest
    needs: [validate, build]
    if: needs.validate.outputs.should-deploy == 'true'
    environment:
      name: staging
      url: https://your-staging-url.com  # Remplacez par votre URL de staging

    steps:
      - name: Download de l'artefact JAR
        uses: actions/download-artifact@v4
        with:
          name: ${{ env.ARTIFACT_NAME }}
          path: ./artifacts

      - name: Préparation de l'artefact pour le déploiement
        run: |
          ls -la ./artifacts/
          # Renomme le JAR avec un nom standardisé
          cd ./artifacts
          JAR_FILE=$(ls *.jar | head -1)
          if [ -z "$JAR_FILE" ]; then
            echo "❌ Aucun fichier JAR trouvé"
            exit 1
          fi
          
          mv "$JAR_FILE" "${{ env.ARTIFACT_NAME }}.jar"
          echo "✅ Artefact préparé: ${{ env.ARTIFACT_NAME }}.jar"
          
          # Vérification de l'intégrité
          echo "📊 Taille de l'artefact: $(du -h ${{ env.ARTIFACT_NAME }}.jar)"
          
          # Création d'un checksum pour vérification
          sha256sum "${{ env.ARTIFACT_NAME }}.jar" > "${{ env.ARTIFACT_NAME }}.jar.sha256"
          echo "🔐 Checksum généré"

      - name: Test de connectivité SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USERNAME }}
          key: ${{ secrets.VPS_SSH_PRIVATE_KEY }}
          script: |
            echo "✅ Connexion SSH établie"
            echo "📂 Espace disque disponible:"
            df -h ${{ env.DEPLOY_PATH }}
            echo "🔧 Vérification des permissions:"
            ls -la ${{ env.DEPLOY_PATH }}/

      - name: Upload des artefacts sur le VPS
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USERNAME }}
          key: ${{ secrets.VPS_SSH_PRIVATE_KEY }}
          source: "./artifacts/${{ env.ARTIFACT_NAME }}.jar,./artifacts/${{ env.ARTIFACT_NAME }}.jar.sha256"
          target: "${{ env.DEPLOY_PATH }}/new_artifact/"
          strip_components: 2  # Supprime "./artifacts/" du chemin
          overwrite: true

      - name: Vérification de l'intégrité sur le serveur
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USERNAME }}
          key: ${{ secrets.VPS_SSH_PRIVATE_KEY }}
          script: |
            cd ${{ env.DEPLOY_PATH }}/new_artifact/
            echo "🔍 Vérification de l'intégrité de l'artefact:"
            
            if [ ! -f "${{ env.ARTIFACT_NAME }}.jar" ]; then
              echo "❌ Artefact non trouvé"
              exit 1
            fi
            
            if [ -f "${{ env.ARTIFACT_NAME }}.jar.sha256" ]; then
              if sha256sum -c "${{ env.ARTIFACT_NAME }}.jar.sha256"; then
                echo "✅ Intégrité de l'artefact vérifiée"
              else
                echo "❌ Échec de la vérification d'intégrité"
                exit 1
              fi
            fi
            
            echo "📊 Informations sur l'artefact:"
            ls -la "${{ env.ARTIFACT_NAME }}.jar"

      - name: Backup de l'ancienne version
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USERNAME }}
          key: ${{ secrets.VPS_SSH_PRIVATE_KEY }}
          script: |
            # Créer un backup de l'ancienne version si elle existe
            if [ -f "${{ env.DEPLOY_PATH }}/current/${{ env.ARTIFACT_NAME }}.jar" ]; then
              BACKUP_DIR="${{ env.DEPLOY_PATH }}/backups/$(date +%Y%m%d_%H%M%S)"
              mkdir -p "$BACKUP_DIR"
              cp "${{ env.DEPLOY_PATH }}/current/${{ env.ARTIFACT_NAME }}.jar" "$BACKUP_DIR/"
              echo "💾 Backup créé dans: $BACKUP_DIR"
            
              # Nettoyer les anciens backups (garder seulement les 5 derniers)
              cd "${{ env.DEPLOY_PATH }}/backups"
              ls -t | tail -n +6 | xargs -r rm -rf
              echo "🧹 Anciens backups nettoyés"
            fi

      - name: Exécution du script de déploiement
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USERNAME }}
          key: ${{ secrets.VPS_SSH_PRIVATE_KEY }}
          script: |
            # Export des variables d'environnement pour le script de déploiement
            export ARTIFACT_NAME="${{ env.ARTIFACT_NAME }}"
            export DEPLOY_PATH="${{ env.DEPLOY_PATH }}"
            export BUILD_NUMBER="${{ github.run_number }}"
            export COMMIT_SHA="${{ github.sha }}"
            export BRANCH_NAME="${{ github.ref_name }}"
            
            # Vérification de l'existence du script de déploiement
            if [ ! -f "${{ env.DEPLOY_PATH }}/deploy_backend.sh" ]; then
              echo "❌ Script de déploiement non trouvé: ${{ env.DEPLOY_PATH }}/deploy_backend.sh"
              exit 1
            fi
            
            # Exécution du script de déploiement
            chmod +x "${{ env.DEPLOY_PATH }}/deploy_backend.sh"
            "${{ env.DEPLOY_PATH }}/deploy_backend.sh"

      - name: Vérification post-déploiement
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USERNAME }}
          key: ${{ secrets.VPS_SSH_PRIVATE_KEY }}
          script: |
            # Attendre que l'application démarre
            echo "⏳ Attente du démarrage de l'application..."
            sleep 30
            
            # Vérification simple du processus (adapter selon votre setup)
            if pgrep -f "${{ env.ARTIFACT_NAME }}.jar" > /dev/null; then
              echo "✅ Application en cours d'exécution"
            else
              echo "⚠️  Processus de l'application non détecté (vérification manuelle recommandée)"
            fi
            
            # Affichage des logs récents (si disponibles)
            LOG_FILE="${{ env.DEPLOY_PATH }}/logs/application.log"
            if [ -f "$LOG_FILE" ]; then
              echo "📋 Dernières lignes des logs:"
              tail -10 "$LOG_FILE"
            fi

  # Job de notification (optionnel)
  notify:
    runs-on: ubuntu-latest
    needs: [validate, build, deploy]
    if: always()
    steps:
      - name: Notification de fin de déploiement
        run: |
          if [ "${{ needs.deploy.result }}" == "success" ]; then
            echo "🎉 Déploiement réussi de SVS Maritime sur staging"
            echo "📊 Build #${{ github.run_number }}"
            echo "🔗 Commit: ${{ github.sha }}"
          elif [ "${{ needs.deploy.result }}" == "failure" ]; then
            echo "❌ Échec du déploiement"
          elif [ "${{ needs.deploy.result }}" == "skipped" ]; then
            echo "⏭️ Déploiement ignoré (branch ou condition non remplie)"
          fi